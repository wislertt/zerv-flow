name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/shared-pr-unlock.yml@19299eac1a2c1b20717f1bf5c1f2a19ffe17f741 # v0.8.14
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"

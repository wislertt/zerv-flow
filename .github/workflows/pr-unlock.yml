name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/shared-pr-unlock.yml@9d4743e3a3dade2898341dbd3baaf1762e53afbf # v0.8.29
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"

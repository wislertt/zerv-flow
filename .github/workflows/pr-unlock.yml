name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/shared-pr-unlock.yml@9355b2fb56554972f51337339b8a47c3358ae736 # v0.8.19
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"
